<Page x:ConnectionId='1'
    x:Class="StreaminApp1.UWP.Views.Movies.MovieInfoPageAdmin"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StreaminApp1.UWP.Views.Movies"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:icon="using:Windows.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <!-- Define an ImageBrush with the desired image source and opacity -->
        <ImageBrush x:ConnectionId='2' x:Key="BackgroundImage"                                                        Opacity="0.5"/>
    </Page.Resources>

    <Border Background="{StaticResource BackgroundImage}">
        <Grid RowSpacing="20" Padding="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />

            </Grid.RowDefinitions>

            <TextBlock x:ConnectionId='3' Grid.Row="0"                                          Style="{StaticResource TitleTextBlockStyle}" FontSize="30" FontWeight="SemiBold" Foreground="White" Margin="0,50,0,0"/>

            <!-- Your existing content -->

            <Grid Grid.Row="1" Margin="0,150,0,0">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:ConnectionId='6' Grid.Column="0"                                              Foreground="LightGray"/>
                <TextBlock x:ConnectionId='7' Grid.Column="1"                                              Foreground="LightGray"/>


            </Grid>

            <Grid Grid.Row="2">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:ConnectionId='5' Grid.Column="0"                                            Foreground="LightGray"/>
                <TextBlock Grid.Column="1">/10</TextBlock>


            </Grid>

           

                <Button Grid.Row="3" Width="80" Height="50" CornerRadius="5" >
                    <TextBlock FontWeight="SemiBold">Watch</TextBlock>
                </Button>



            <TextBlock x:ConnectionId='4'  Grid.Row="4"                                                  Margin="0,20,0,0" Foreground="LightGray"/>





        </Grid>
    </Border>
</Page>






